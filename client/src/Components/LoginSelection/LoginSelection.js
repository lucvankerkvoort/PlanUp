import React from "react";
import Client from "./LoginForm";

const LoginSelection = () => {
  return (
    <div className="login-selection">
      <button>User</button>
      <button>Client</button>
    </div>
  );
};

export default LoginSelection;
